var { a_star_search } = require('./A*Algo')
var { bfs } = require('./BFS')
var { dfs } = require('./DFS')
var { greedy_search } = require('./GreedySearch')
var { ucs } = require('./UCS')

module.exports = {
  a_star_search,
  bfs,
  dfs,
  greedy_search,
  ucs
}
